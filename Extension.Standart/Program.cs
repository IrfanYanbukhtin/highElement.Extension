namespace Extension.Standart_
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //int a = int.Parse(Console.ReadLine());
            //Console.WriteLine(a.Square());

            var arr = new ArrayHighElement();
            arr.EnterArray();

        }
    }
}